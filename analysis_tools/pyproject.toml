[project]
name = "chip-performance-analysis"
version = "0.1.0"
description = "Reusable performance analysis algorithms for the AI chip platform"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/chip_performance_analysis"]
