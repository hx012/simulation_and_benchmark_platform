# 推荐开发方式：Frontend 与 FastAPI 运行在同一台 Linux 服务器。
# 浏览器请求 /api，Vite 在服务器内部代理到 FastAPI，避免浏览器 CORS。
VITE_API_BASE_URL=
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# V1.3 登录页默认工号，仅用于开发阶段预填；真实 SSO / LDAP 尚未接入。
VITE_DEFAULT_OWNER_ID=test-user
